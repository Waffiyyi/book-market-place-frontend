/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['example-image.waffiyyi.com', "covers.openlibrary.org", "res.cloudinary.com"],
  },
};
export default nextConfig;
